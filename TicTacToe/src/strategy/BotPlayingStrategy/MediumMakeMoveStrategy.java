package strategy.BotPlayingStrategy;

import models.Board;
import models.Move;

public class MediumMakeMoveStrategy implements BotMakeMoveStrategy{
    @Override
    public Move makemove(Board board) {
        return null;
    }
}
